# Copy to .env and fill in. In Portainer, set these as stack environment variables instead.
# These are the three inputs to the QR algorithm; the deviceId is the sensitive one.
# Use your OWN test-account values here; do not commit real values.
BF_CARD_NUMBER=V00123456
BF_DEVICE_ID=your-device-id-here
BF_CONSTANT=XYZ
